---
title: "Repository Structure"
description: "Every file in the v8s.link reference repository, what it does, and what you need to change."
nav_order: 31
---

The [v8s.link repository](https://github.com/vanityURLs/v8s.link) was generated from the `vanityURLs/vanityURLs` template. Here is every file, annotated.

{{< filetree/container >}}
{{< filetree/folder name="v8s.link" >}}
{{< filetree/folder name=".github" >}}
{{< filetree/folder name="workflows" >}}
{{< filetree/file name="deploy.yml" annotation="// CI/CD — triggers on push to main" >}}
{{< /filetree/folder >}}
{{< /filetree/folder >}}
{{< filetree/folder name="build" annotation="// committed generated output" >}}
{{< filetree/file name="_headers" annotation="// HTTP headers served by Cloudflare" >}}
{{< filetree/file name="_redirects" annotation="// generated by make build / CI" >}}
{{< /filetree/folder >}}
{{< filetree/folder name="scripts" annotation="// CLI tools installed by make setup" >}}
{{< filetree/file name="lnk" annotation="// redirect management CLI" >}}
{{< filetree/file name="validateURL" annotation="// URL reachability checker" >}}
{{< /filetree/folder >}}
{{< filetree/file name=".gitignore" >}}
{{< filetree/file name="LICENSE" annotation="// MIT" >}}
{{< filetree/file name="Makefile" annotation="// setup, build, config targets" >}}
{{< filetree/file name="README.md" annotation="// update for your domain" >}}
{{< filetree/file name="dynamic.lnk" annotation="// temporary / campaign links" >}}
{{< filetree/file name="logo.png" annotation="// optional branding, not used by Cloudflare" >}}
{{< filetree/file name="static.lnk" annotation="// permanent links" >}}
{{< filetree/file name="vanityURLs.conf" annotation="// your local config — never commit secrets" >}}
{{< /filetree/folder >}}
{{< /filetree/container >}}

## File-by-file guide

### `vanityURLs.conf` — your local configuration

The only file you must edit before running `make setup`. It is read by the Makefile and by the `lnk` script. See the [full configuration reference](/docs/demo/configuration/).

```bash
SCRIPT_DIR=~/.config/bin
REPO_DIR="/Volumes/Tarmac/codePublic/vanityURLs/v8s.link"   # ← change this
MY_DOMAIN="v8s.link"            # ← your domain
MY_PAGE="v8s-link.pages.dev"    # ← your Cloudflare Pages URL
SHORTCODE_LENGTH=3
#DRY_RUN=true
```

### `static.lnk` — permanent redirects

Plain text, one rule per line. See [Link Examples](/docs/demo/links/) for the full annotated content.

### `dynamic.lnk` — temporary / campaign redirects

Same format as `static.lnk`. The v8s.link deployment uses one splat rule here:

```
/github/*  https://github.com/vanityURLs/:splat
```

This means `v8s.link/github/vanityURLs` → `github.com/vanityURLs/vanityURLs`, `v8s.link/github/website` → `github.com/vanityURLs/website`, and so on.

### `build/_redirects` — generated, but committed

Produced by `cat static.lnk dynamic.lnk > build/_redirects`. Cloudflare Pages reads it from the `build/` output directory. **The file is committed** so Cloudflare Pages can serve it without running a build command.

{{< callout type="tip" title="Why commit the generated file?" >}}
Cloudflare Pages' build command is `cat static.lnk dynamic.lnk > build/_redirects`. Committing the output means the repo always shows the current state of all redirects — useful for code review and rollback.
{{< /callout >}}

### `build/_headers` — generated by `make setup`, then committed

Created once by `make setup` based on your `MY_DOMAIN` and `MY_PAGE`. See the [DNS Setup page](/docs/demo/dns/) for context on why two zones need separate header rules.

```
https://v8s-link.pages.dev/*
  X-Robots-Tag: noindex
  X-Content-Type-Options: nosniff

https://v8s.link/*
  X-Robots-Tag: noindex
  X-Content-Type-Options: nosniff
```

{{< callout type="warning" title="Update _headers if you change your domain" >}}
Run `make headers` any time `MY_DOMAIN` or `MY_PAGE` changes. The file is not regenerated automatically.
{{< /callout >}}

### `scripts/lnk` — the CLI

The primary tool for managing redirects. Installed to `SCRIPT_DIR` by `make setup`. See the [Commands reference](/docs/commands/).

### `scripts/validateURL` — URL reachability checker

A companion script that performs live HTTP checks on redirect destinations. Called by `lnk validate --live`. Can also be used standalone:

```bash
validateURL https://github.com/vanityURLs
# → 200 OK (87ms) ✓
```

### `Makefile` — task runner

Key targets:

| Target | What it does |
|--------|-------------|
| `make config` | Open `vanityURLs.conf` in `$EDITOR` (defaults to `vi`) |
| `make setup` | Install scripts, generate `_headers`, create starter link files |
| `make build` | Local build: `cat static.lnk dynamic.lnk > build/_redirects` |
| `make debug` | Print `MY_DOMAIN` to verify config is loaded |

### `.github/workflows/` — CI/CD

The deploy workflow triggers on every push to `main` and runs the build command. Cloudflare Pages detects the push via webhook and serves the updated `build/` directory within ~15 seconds.
